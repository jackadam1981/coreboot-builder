name: 7-Kaisa MrChromebox (EDK2 PXE + iPXE)

on:
  workflow_dispatch:
    inputs:
      release:
        description: "创建 Release 发布 / Create Release"
        required: false
        type: boolean
        default: true
      release_name:
        description: "Release 名称 / Release Name"
        required: false
        type: string
        default: "Coreboot Kaisa - 方案7 (Integrated iPXE)"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出构建仓库 / Checkout Builder Repository
        uses: actions/checkout@v4

      - name: 拉取 Coreboot SDK 容器 / Pull Coreboot SDK
        run: docker pull coreboot/coreboot-sdk:latest

      - name: 克隆 MrChromebox Coreboot 源码 / Clone Coreboot Source
        run: |
          echo "📥 克隆 MrChromebox coreboot"
          git clone https://github.com/MrChromebox/coreboot.git coreboot
          cd coreboot
          git submodule update --init --checkout --recursive

      # 由于改用 EDK2 内置 PXE/HTTP 网络支持，不再准备或注入外部 iPXE 文件
      # 这里保留构建前的源码拉取与配置步骤

      - name: 配置 PXE ROM 支持 / Configure PXE ROM Support
        run: |
          echo "📝 添加 PXE ROM 支持配置"
          cd coreboot

          # 定义配置项数组
          PXE_CONFIGS=(
            "CONFIG_EDK2_LOAD_OPTION_ROMS=y"
          )
          
          # 简化 EDK2 配置，专注于 iPXE 集成
          # 只保留基本的 UEFI 功能，网络功能由 iPXE 提供
          EDK2_BUILD_PARAMS="-D NETWORK_DRIVER_ENABLE=FALSE"
          EDK2_BUILD_PARAMS="$EDK2_BUILD_PARAMS -D NETWORK_ENABLE=FALSE"
          
          # 添加到配置数组
          PXE_CONFIGS+=("CONFIG_EDK2_CUSTOM_BUILD_PARAMS=\"$EDK2_BUILD_PARAMS\"")
          
          INTEL_CONFIGS=(

          )
          
          # 写入 PXE ROM 支持配置
            echo "" >> configs/cml/config.kaisa.uefi
            echo "# PXE ROM 支持配置" >> configs/cml/config.kaisa.uefi
          for config in "${PXE_CONFIGS[@]}"; do
            echo "$config" >> configs/cml/config.kaisa.uefi
          done
            echo "" >> configs/cml/config.kaisa.uefi
          
          # 写入 Intel 芯片组系统稳定配置
          if [ ${#INTEL_CONFIGS[@]} -gt 0 ]; then
            echo "# Intel 芯片组系统稳定配置（适合 Kaisa 主板）" >> configs/cml/config.kaisa.uefi
            for config in "${INTEL_CONFIGS[@]}"; do
              echo "$config" >> configs/cml/config.kaisa.uefi
            done
            echo "" >> configs/cml/config.kaisa.uefi
          fi
          
          # 保存配置到环境变量供后续使用
          echo "PXE_CONFIGS=${PXE_CONFIGS[*]}" >> $GITHUB_ENV
          echo "INTEL_CONFIGS=${INTEL_CONFIGS[*]}" >> $GITHUB_ENV
          
          # 为 Release body 生成格式化的配置列表
          PXE_CONFIG_LIST=""
          for config in "${PXE_CONFIGS[@]}"; do
            PXE_CONFIG_LIST="${PXE_CONFIG_LIST}- ${config}"$'\n'
          done
          echo "PXE_CONFIG_LIST<<EOF" >> $GITHUB_ENV
          echo "${PXE_CONFIG_LIST}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          
          INTEL_CONFIG_LIST=""
          if [ ${#INTEL_CONFIGS[@]} -gt 0 ]; then
            for config in "${INTEL_CONFIGS[@]}"; do
              INTEL_CONFIG_LIST="${INTEL_CONFIG_LIST}- ${config}"$'\n'
            done
            echo "INTEL_CONFIG_LIST<<EOF" >> $GITHUB_ENV
            echo "${INTEL_CONFIG_LIST}" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          else
            echo "INTEL_CONFIG_LIST=" >> $GITHUB_ENV
          fi
          
          # 打印最终的配置文件内容
          echo "📋 最终配置文件内容:"
          echo "===================="
          cat configs/cml/config.kaisa.uefi
          echo "===================="

      - name: 验证 iPXE EFI 文件
        run: |
          echo "=== 验证 iPXE EFI 文件 ==="
          echo "检查项目根目录中的 rtl8168.efi..."
          echo ""
          
          # 检查 iPXE 文件是否存在
          if [ -f "./rtl8168.efi" ]; then
            echo "✅ 找到项目中的 rtl8168.efi"
            ls -lh ./rtl8168.efi
            echo "✅ iPXE 文件验证完成"
          else
            echo "❌ 未找到 ./rtl8168.efi"
            echo "请确保 iPXE 文件已放置在项目根目录"
            exit 1
          fi

      - name: 📦 编译 MrChromebox Coreboot 固件 / Build Coreboot
        run: |
          echo "🔨 编译 MrChromebox coreboot（kaisa 主板）"
          
          # 使用 MrChromebox 标准编译流程（参考 build-coreboot.yml）
          # 确保输出目录权限正确
          mkdir -p roms
          chmod 755 roms
          
          # 使用 MrChromebox 标准编译流程
          docker run --rm --user root \
            -v ${{ github.workspace }}/coreboot:/home/coreboot/coreboot \
            -v ${{ github.workspace }}/roms:/home/coreboot/roms \
            -w /home/coreboot/coreboot \
            coreboot/coreboot-sdk:latest \
            bash -c "git config --global --add safe.directory /home/coreboot/coreboot && \
                     # 使用 MrChromebox 编译脚本（配置已在配置文件中）
                     echo '🔧 使用 MrChromebox build-uefi.sh 编译 kaisa...' && \
                     ./build-uefi.sh kaisa && \
                       chmod 644 /home/coreboot/roms/*.rom && \
                     echo '✅ MrChromebox 编译完成'"

      - name: 集成 iPXE 到 ROM
        run: |
          echo "=== 集成 iPXE 到 ROM ==="
          echo "将 rtl8168.efi 添加到 ROM 中..."
          echo ""
          
          # 检查 ROM 文件
          ROM_FILE=$(ls roms/coreboot_*.rom | head -1)
          if [ -z "$ROM_FILE" ]; then
            echo "❌ 未找到 ROM 文件"
            exit 1
          fi
          
          echo "✅ 找到 ROM 文件: $(basename "$ROM_FILE")"
          
          # 创建可写的 ROM 副本
          echo "🔧 创建可写的 ROM 副本..."
          ROM_COPY="./coreboot_edk2-kaisa-mrchromebox_work.rom"
          cp "$ROM_FILE" "$ROM_COPY"
          chmod 644 "$ROM_COPY"
          ls -lh "$ROM_COPY"
          
          # 检查 iPXE 文件
          if [ ! -f "./rtl8168.efi" ]; then
            echo "❌ 未找到 rtl8168.efi 文件"
            exit 1
          fi
          
          echo "✅ 找到 iPXE 文件: rtl8168.efi"
          ls -lh ./rtl8168.efi
          
          # 使用 cbfstool 添加 iPXE 到 ROM
          CBFSTOOL="${{ github.workspace }}/tools/cbfstool"
          chmod +x "$CBFSTOOL"
          
          echo ""
          echo "🔧 添加 iPXE 到 ROM..."
          
          # 添加 iPXE 到 ROM，供 EFI Shell 使用
          echo "🔧 添加 iPXE 到 ROM 供 EFI Shell 使用..."
          
          # 添加到 CBFS 中，供 EFI Shell 访问
          if "$CBFSTOOL" "$ROM_COPY" add -f ./rtl8168.efi -n ipxe.efi -t raw; then
            echo "✅ iPXE 已添加到 CBFS (ipxe.efi)"
          else
            echo "⚠️ 直接添加失败，尝试 LZMA 压缩..."
            if "$CBFSTOOL" "$ROM_COPY" add -f ./rtl8168.efi -n ipxe.efi -t raw -c lzma; then
              echo "✅ iPXE 已添加到 CBFS (ipxe.efi, LZMA 压缩)"
            else
              echo "❌ iPXE 添加失败"
              exit 1
            fi
          fi
          
          echo ""
          echo "ℹ️ 注意：iPXE 已添加到 CBFS 中，需要通过以下方式使用："
          echo "   1. 在 EFI Shell 中执行：fs0:\uefi-boot-setup.nsh"
          echo "   2. 或直接执行：fs0:\ipxe.efi"
          
          # 添加 UEFI 启动设置脚本
          echo ""
          echo "🔧 添加 UEFI 启动设置脚本..."
          if [ -f "./uefi-boot-setup.nsh" ]; then
            if "$CBFSTOOL" "$ROM_COPY" add -f ./uefi-boot-setup.nsh -n uefi-boot-setup.nsh -t raw; then
              echo "✅ UEFI 启动设置脚本已添加"
            else
              echo "⚠️ UEFI 启动设置脚本添加失败"
            fi
          fi
          
          # 添加调试脚本
          if [ -f "./efi-debug-script-v2.nsh" ]; then
            if "$CBFSTOOL" "$ROM_COPY" add -f ./efi-debug-script-v2.nsh -n efi-debug-script-v2.nsh -t raw; then
              echo "✅ 调试脚本已添加"
            else
              echo "⚠️ 调试脚本添加失败"
            fi
          fi
          
          if [ -f "./network-boot-diagnostics.nsh" ]; then
            if "$CBFSTOOL" "$ROM_COPY" add -f ./network-boot-diagnostics.nsh -n network-boot-diagnostics.nsh -t raw; then
              echo "✅ 网络诊断脚本已添加"
            else
              echo "⚠️ 网络诊断脚本添加失败"
            fi
          fi
          
          # 将修改后的 ROM 移动到 roms 目录
          echo ""
          echo "🔧 将修改后的 ROM 移动到 roms 目录..."
          ROM_FINAL="roms/coreboot_edk2-kaisa-mrchromebox_with_ipxe.rom"
          cp "$ROM_COPY" "$ROM_FINAL"
          echo "✅ 修改后的 ROM 已保存为: $(basename "$ROM_FINAL")"
          
          # 显示文件信息
          echo ""
          echo "📦 最终 ROM 文件信息:"
          ls -lh "$ROM_FINAL"
          
          # 清理临时文件
          rm -f "$ROM_COPY"
          echo "✅ 临时文件已清理"
          
          echo ""
          echo "✅ iPXE 集成完成"

      - name: 验证编译结果 / Verify Build Results
        run: |
          echo "📦 验证编译结果"

          # 检查原始 ROM 文件
          ROM_FILE=$(ls roms/coreboot_edk2-kaisa-mrchromebox_*.rom | grep -v with_ipxe | head -1)
          if [ -z "$ROM_FILE" ]; then
            echo "❌ 未找到原始 ROM 文件"
            ls -la roms/
            exit 1
          fi
          
          echo "✅ 找到原始 ROM 文件: $(basename "$ROM_FILE")"
          ls -lh "$ROM_FILE"
          
          # 检查集成 iPXE 的 ROM 文件
          ROM_IPXE_FILE="roms/coreboot_edk2-kaisa-mrchromebox_with_ipxe.rom"
          if [ -f "$ROM_IPXE_FILE" ]; then
            echo "✅ 找到集成 iPXE 的 ROM 文件: $(basename "$ROM_IPXE_FILE")"
            ls -lh "$ROM_IPXE_FILE"
          else
            echo "❌ 未找到集成 iPXE 的 ROM 文件"
            exit 1
          fi
          
          # 使用仓库内 tools/cbfstool 打印 ROM 结构（仅展示，不作为成败依据）
          echo "📦 使用本仓库 tools/cbfstool 打印集成 iPXE 的 ROM 结构"
          CBFSTOOL="${{ github.workspace }}/tools/cbfstool"
          chmod +x "$CBFSTOOL" 2>/dev/null || true
          "$CBFSTOOL" "$ROM_IPXE_FILE" print || echo "⚠️ cbfstool 打印失败（不影响构建产物上传）"
          
          # 使用 cbfstool 获取准确的空间信息
          echo "🔍 使用 cbfstool 分析 ROM 空间使用情况"
          
          # 获取 ROM 总大小
          ROM_SIZE=$(stat -c%s "$ROM_IPXE_FILE")
          ROM_SIZE_MB=$((ROM_SIZE / 1024 / 1024))
          echo "📦 ROM 总大小: ${ROM_SIZE} 字节 (${ROM_SIZE_MB} MB)"
          
          # 使用 cbfstool 获取 COREBOOT 区域的容量和使用情况
          echo "📊 CBFS (COREBOOT) 空间分析:"
          CBFS_INFO=$("$CBFSTOOL" "$ROM_IPXE_FILE" print 2>/dev/null || echo "")
          
          if [ -n "$CBFS_INFO" ]; then
            # 提取 COREBOOT 区域容量信息
            CBFS_CAPACITY=$(echo "$CBFS_INFO" | grep "CBFS.*capacity" | awk '{print $NF}' | head -1)
            if [ -n "$CBFS_CAPACITY" ]; then
              CBFS_CAPACITY_KB=$((CBFS_CAPACITY / 1024))
              echo "🏗️  CBFS 总容量: ${CBFS_CAPACITY} 字节 (${CBFS_CAPACITY_KB} KB)"
            fi
            
            # 计算已使用空间（所有非 empty/null 条目）
            USED_SIZE=$(echo "$CBFS_INFO" | grep -v -E "(empty|null|CBFS.*capacity)" | awk '{sum += $4} END {print sum+0}')
            if [ -n "$USED_SIZE" ] && [ "$USED_SIZE" -gt 0 ]; then
              USED_SIZE_KB=$((USED_SIZE / 1024))
              echo "📈 已使用空间: ${USED_SIZE} 字节 (${USED_SIZE_KB} KB)"
            fi
            
            # 计算可用空间（empty 和 null 条目）
            EMPTY_SIZE=$(echo "$CBFS_INFO" | grep -E "(empty|null)" | awk '{sum += $4} END {print sum+0}')
            if [ -n "$EMPTY_SIZE" ] && [ "$EMPTY_SIZE" -gt 0 ]; then
              EMPTY_SIZE_KB=$((EMPTY_SIZE / 1024))
              echo "💾 可用空间: ${EMPTY_SIZE} 字节 (${EMPTY_SIZE_KB} KB)"
              
              # 计算使用率
              if [ -n "$CBFS_CAPACITY" ] && [ "$CBFS_CAPACITY" -gt 0 ]; then
                USAGE_PERCENT=$(( (USED_SIZE * 100) / CBFS_CAPACITY ))
                echo "📊 空间使用率: ${USAGE_PERCENT}%"
              fi
            else
              echo "⚠️  未发现可用空间条目"
            fi
            
            # 显示最大的可用空间块
            LARGEST_EMPTY=$(echo "$CBFS_INFO" | grep -E "(empty|null)" | awk '{print $4}' | sort -nr | head -1)
            if [ -n "$LARGEST_EMPTY" ] && [ "$LARGEST_EMPTY" -gt 0 ]; then
              LARGEST_EMPTY_KB=$((LARGEST_EMPTY / 1024))
              echo "🎯 最大可用块: ${LARGEST_EMPTY} 字节 (${LARGEST_EMPTY_KB} KB)"
            fi
          else
            echo "❌ 无法获取 CBFS 信息"
          fi

      # 删除外部 iPXE 注入步骤：改为完全依赖 EDK2 内置网络栈

      - name: 重新生成校验文件 / Regenerate Checksums
        run: |
          echo "🧮 重新生成 ROM 校验 (SHA1/SHA256)"
          ROM_FILE=$(ls roms/coreboot_*.rom | head -1)
          if [ -z "$ROM_FILE" ]; then
            echo "❌ 未找到 ROM 文件，无法生成校验"
            exit 1
          fi
          ROM_BASE=$(basename "$ROM_FILE")
          
          # 清理旧的校验文件，避免引用未修改前的校验
          rm -f "roms/${ROM_BASE}.sha1" "roms/${ROM_BASE}.sha256" || true
          
          # 在 roms/ 目录下生成标准 sha1sum/sha256sum 输出格式
          (cd roms && sha1sum "$ROM_BASE" > "${ROM_BASE}.sha1")
          (cd roms && sha256sum "$ROM_BASE" > "${ROM_BASE}.sha256")
          
          # 调试输出：打印计算得到的 SHA1 与文件内记录的 SHA1，用于校验比对
          COMPUTED_SHA1=$(cd roms && sha1sum "$ROM_BASE" | awk '{print $1}')
          FILE_SHA1=$(awk '{print $1}' "roms/${ROM_BASE}.sha1")
          echo "🔎 DEBUG SHA1 computed=${COMPUTED_SHA1} file=${FILE_SHA1}"
          if [ "$COMPUTED_SHA1" = "$FILE_SHA1" ]; then
            echo "✅ SHA1 一致"
          else
            echo "❗ SHA1 不一致（请检查生成流程）"
          fi
          
          echo "✅ 已生成: roms/${ROM_BASE}.sha1 与 roms/${ROM_BASE}.sha256"

      - name: 上传固件 / Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: coreboot-kaisa-edk2-pxe-${{ github.run_number }}
          path: |
            roms/*.rom
            roms/*.sha1
            roms/*.sha256
            integration_report.md

      - name: 创建 Release / Create Release
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.release) || (github.event_name != 'workflow_dispatch') }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: kaisa-edk2-pxe-ipxe-${{ github.run_number }}
          name: "${{ inputs.release_name || 'Coreboot Kaisa - 方案7 (EDK2 PXE + iPXE)' }} #${{ github.run_number }}"
          body: |
           # 🎯 方案7：MrChromebox EDK2 + iPXE 混合网络启动
           
           ## 🔧 固件配置
           - **EDK2 网络功能**：已禁用（由 iPXE 提供网络功能）
           - **Option ROM 加载**：CONFIG_EDK2_LOAD_OPTION_ROMS=y
           - **简化配置**：专注于 iPXE 集成，减少固件复杂度
           
           ## 🔧 iPXE 集成功能
           - **自编译 iPXE**：使用您编译的 rtl8168.efi 驱动
           - **CBFS 集成**：iPXE 已集成到固件 ROM 中
           - **独立 ROM 文件**：提供集成 iPXE 的独立 ROM 文件
           - **多种启动方式**：支持 UEFI 启动菜单、EFI Shell 手动启动
           - **调试工具**：包含完整的网络诊断和启动设置脚本
           
           ## 📦 文件说明
           - **原始 ROM**：`coreboot_edk2-kaisa-mrchromebox_*.rom`（不含 iPXE）
           - **集成 iPXE ROM**：`coreboot_edk2-kaisa-mrchromebox_with_ipxe.rom`（推荐使用）
           
           ${{ env.PXE_CONFIG_LIST }}
           
           ## 🚀 使用方法
           
           ### 方法1：EFI Shell 设置启动项（推荐）
           1. 刷入固件后，在 UEFI 启动菜单中选择 "UEFI Shell"
           2. 在 EFI Shell 中执行：
              ```
              fs0:\uefi-boot-setup.nsh
              ```
           3. 重启系统，在启动菜单中选择 "iPXE Network Boot"
           
           ### 方法2：直接执行 iPXE
           1. 在 EFI Shell 中执行：
              ```
              fs0:\ipxe.efi
              ```
           
           ### 方法3：手动添加启动项
           1. 在 EFI Shell 中执行：
              ```
              bcfg boot add 0 fs0:\ipxe.efi "iPXE Network Boot"
              ```
           2. 重启系统，在启动菜单中选择 "iPXE Network Boot"
           
           ## 🔧 调试工具
           - **efi-debug-script-v2.nsh**：增强版 EFI Shell 调试脚本，专门优化网络启动诊断
           - **network-boot-diagnostics.nsh**：专业网络启动诊断脚本，专注于 PXE/HTTP Boot 问题排查
           - **uefi-boot-setup.nsh**：iPXE 启动菜单设置脚本（备选方案）
           
           ${{ env.INTEL_CONFIG_LIST != '' && format('同时追加 Intel 芯片组稳定性相关配置（适配 Kaisa 主板）：\n{0}', env.INTEL_CONFIG_LIST) || '' }}
            
            **构建时间**: ${{ github.event.repository.updated_at }}
          files: |
            roms/*.rom
            roms/*.sha1
            roms/*.sha256
            integration_report.md
            efi-debug-script-v2.nsh
            network-boot-diagnostics.nsh
            uefi-boot-setup.nsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
